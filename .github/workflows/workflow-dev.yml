permissions:
  contents: read
  packages: write

name: Build Workflow

on: [push]

jobs:

  Build:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Build image
        run: |
          docker build -t ghcr.io/taro-common/farstforward-cms:${{ github.sha }} .

      - name: Login to GHCR
        run: echo ${{ secrets.GITHUB_TOKEN }} | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Push image
        run: |
          docker push ghcr.io/taro-common/farstforward-cms:${{ github.sha }}

      - name: Tag latest
        run: |
          docker tag ghcr.io/taro-common/farstforward-cms:${{ github.sha }} ghcr.io/taro-common/farstforward-cms:latest
          docker push ghcr.io/taro-common/farstforward-cms:latest
